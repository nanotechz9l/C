/* tells the compiler to include information about the standard input/output library */
#include <stdio.h>

/* c programs begin executing at the beginning of
main. every program must have a main somewhere. */
main() 

{
	printf("hello, world!\n");
}